import { SoftColorDirective } from './soft-color.directive';

describe('SoftColorDirective', () => {
  it('should create an instance', () => {
    const directive = new SoftColorDirective();
    expect(directive).toBeTruthy();
  });
});
